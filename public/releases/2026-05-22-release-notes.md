## In-app updates

- The app now checks release notes from the public releases folder after startup.
- New release entries are combined into a single update popup.
- After the popup is dismissed, the newest shown release id is saved locally so old entries stay hidden.
